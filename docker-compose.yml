version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: migrator
      POSTGRES_USER: migrator
      POSTGRES_PASSWORD: migrator
    ports: ["5432:5432"]
  web:
    build: .
    ports: ["8000:8000"]
    volumes: [".:/app"]
